# 글쓰기 스타일 가이드

이 블로그 글의 어투·구성을 일관되게 유지하기 위한 가이드다. 기존 글 27편(article 18, note 9)을 분석해 실제로 써온 패턴을 정리했다. AI에게 초안·교정을 맡길 때 이 문서를 기준으로 삼고, 글을 쓸 때 본인 체크리스트로도 쓴다.

> 글은 두 종류다. **article**은 개념 정리·트러블슈팅 등 본문이 긴 글, **note**는 메모·팁 성격의 짧은 글이다. 코어 보이스는 공유하지만 분량과 톤이 다르므로 섹션을 나눠 정리했다.

---

## 1. 공통 코어 보이스 (article·note 모두)

시기와 무관하게 항상 지키는 규칙이다.

- **종결어미는 평어체(`~다` / `~했다` / `~된다`)로 통일한다.** 경어체(`~습니다`)는 쓰지 않는다.
  - 본문 설명은 현재 단정형(`~한다` / `~된다`), 경험 서술은 과거형(`~했다` / `~였다`).
- **1인칭은 "나 / 내 / 우리(팀)"를 쓴다. "필자" 같은 격식 표현은 쓰지 않는다.**
  - 실무 글에서는 "우리 서비스", "우리 코드", "우리 케이스"가 자연스럽다.
- **백틱 규칙 — "코드나 터미널에 그대로 타이핑할 수 있는 것"에만 백틱을 쓴다(리터럴 코드 토큰).**
  - **백틱 O**: 메서드·함수(`cookies()`, `useState()`), 훅·컴포넌트 이름(`useQuery`, `useEffect`, `Suspense`, `ErrorBoundary`), API·키워드·타입(`await`, `Map`, `Promise`), 파일 경로·명령어·플래그·환경변수(`~/.ssh/config`, `--privileged`, `NODE_TLS_REJECT_UNAUTHORIZED`), 패키지 식별자(`@tanstack/react-query`, `npm install next`), 에러명·코드(`CERT_SIGNATURE_FAILURE`).
  - **백틱 X**: 제품·프레임워크·라이브러리·도구·언어 이름을 고유명사로 부를 때(Next.js, React, Vite, Astro, esbuild, Turbopack, Go, Java), 일반화된 개념어(캐시, 렌더링, 스냅샷, 쓰로틀링).
  - **같은 단어라도 역할로 구분한다.** "Next.js의 App Router는…"(제품 이름 → 백틱 없음) vs "`next build`를 실행하면…"(명령어 → 백틱).
  - **라이브러리는 표기 형태로 구분한다.** 브랜드 고유명사로 부르면 백틱 없음(Framer Motion, Styled Components), 설치·import에 쓰는 패키지 식별자 형태면 백틱(`framer-motion`, `styled-components`, `react-hook-form`).
  - **대소문자는 각 프로젝트의 공식 표기를 따른다.** 헷갈리면 공식 사이트·레포 표기를 확인한다.
    - 대문자 시작: React, Vue, Astro, Vite, Next.js, Node.js, Turbopack, ESLint, Prettier, TypeScript, JavaScript, Go, Java.
    - 소문자(주의): **webpack**, **esbuild**, npm, pnpm, yarn — 공식 브랜딩이 소문자다.
- **한글 용어 규칙:**
  - 일반화된 개념어는 한글 음차/번역: "캐시", "스냅샷", "렌더링", "쓰로틀링", "마이그레이션".
  - 영어 약어는 첫 등장 시 풀네임을 병기한다: RSC(React Server Components), TDZ(Temporal Dead Zone), HMR(Hot Module Replacement).
  - 외래어는 국립국어원 표준 표기를 따른다: 디렉터리(O) / 디렉토리(X), 렌더링(O), 캐시(O).
- **이모지는 쓰지 않는다.** article·note 모두 본문에 이모지를 넣지 않는다.
- **핵심 결론·수치는 굵게(`**`) 강조한다.** 예) `**이미지 페이로드가 909KB → 21.4KB로 90%가 넘게 감소했다!**`
- **영어 인용문은 `>` 인용블록으로 원문을 싣고, 바로 아래 한글 번역을 굵게 붙인다.**
- **주장은 실측으로 뒷받침한다.** 성능·동작 주장은 수치·실험 결과로 받친다(`console.time`, 부하 테스트 표 등). 측정의 한계가 있으면 솔직히 명시한다("로컬 테스트라 프로덕션 변수는 반영되지 않았다").

---

## 2. article 가이드 (긴 글 — 개념 정리 / 트러블슈팅)

### 톤

- **딱딱하거나 격식 있는 어투가 아니라, 부드럽고 친근한 톤으로 쓴다.** 독자에게 설명하듯 말을 거는 느낌을 지향한다. 아래 기법들은 모두 이 부드러운 톤을 만들기 위한 수단이다.
- **수사적 질문을 적극 쓴다.** 헤딩 자체를 질문형으로(`## Map의 성능이 더 좋다?`, `## 왜 스냅샷처럼 작동하는가`), 본문에서도 독자에게 묻고 다음 단락에서 답한다("그렇다면 여러 데이터를 로드하는 경우에는 어떨까?").
- **구어체적 친근함을 허용한다.** 괄호 안 농담·자조를 곳곳에 넣는다. 예) "(베트남에서 롤이 이렇게 인기 있는 스포츠인줄 몰랐다...)" 다만 이모지는 쓰지 않는다(공통 규칙 참고).
- 독자 동행형 표현을 쓴다: "살펴보자", "직접 실험해보자", "보자".

### 도입부

세 유형 중 하나로 시작한다.

1. **실무/프로젝트 상황 제시** (트러블슈팅 글의 기본): "DevOps로부터 … 쓰로틀링이 자주 발생한다는 메시지를 전달받았다."
2. **질문 던지기 또는 `>` 인용블록**: "> 브라우저 주소창에 github.com을 입력했을 때 … 화면에 보이게 되는 걸까?"
3. **개인적 궁금증/동기**: "이 둘의 차이점이 궁금해져서 알아보고 정리하고자 한다."

### 구성

- 헤딩은 `##`(h2)가 주력, 세부 분기는 `###`. `#`(h1)은 제목 전용이므로 본문에 쓰지 않는다.
- 트러블슈팅 글은 **문제 → 원인 분석 → 해결 → 결과 측정 → 결론** 흐름을 따른다.
- 코드블록 언어 태그는 실제 언어로 정확히(`typescript`, `tsx`, `js`, `html`, `css`, `yaml`).
- 코드 주석으로 핵심을 짚는다("// lock 체크 없이 즉시 삭제!"). 발췌는 `// ...생략` 으로 명시한다.
- 실행 순서·타임라인은 ` ```txt ` 블록에 ASCII로 그린다.
- 이미지 alt는 파일명이 아니라 **설명형 캡션**으로 쓴다: `![피크 시간대 요청 지표 — 최대 383 req/s](request_metric.png)`
- 마무리 헤딩은 **`## 마무리`** 하나로 통일한다. (핵심 재요약이든 소감·교훈이든 헤딩 명칭은 동일)
- 개념 글은 끝에 `## 참고` 섹션으로 MDN·공식문서·StackOverflow 링크를 단다. 트러블슈팅 글은 본문에 소스코드 링크를 인라인으로 건다.

### frontmatter

```mdx
export const metadata = {
  title: "글 제목",
  description: "한 줄 요약 — 글의 핵심 주장",
  createdAt: "YYYY-MM-DD",
  tags: ["Next.js"],
};
```

article은 `description`을 **반드시** 포함한다.

---

## 3. note 가이드 (짧은 글 — 메모 / 팁)

note의 간결함은 "산문을 줄여서"가 아니라 **산문을 최소한으로 두고 코드블록·불릿으로 본문을 대체**하는 데서 나온다.

### 톤 (article과의 결정적 차이)

- **수사적 질문 금지.** 독자에게 묻지 않고 건조하게 서술한다. (이모지는 article·note 모두 쓰지 않으므로 공통 규칙 참고)
- **화자 개입·배경 설명을 최소화한다.** "한 번쯤 들어봤을 것이다" 류의 도입 없이 바로 본론.
- 감정이 드러나는 유일한 허용 표현은 괄호 감탄 정도다: "(클라이언트와 서버가 같은 도메인임에도!)"

### 길이

- 한 화면(약 20~40줄)에 끝낸다. 산문은 1~4문단, 헤딩은 0~2개.
- 옵션·설정값 설명은 산문이 아니라 **콜론 불릿**으로 압축한다:
  - `--privileged`: 컨테이너가 모든 호스트 장치에 접근하도록 권한을 부여
  - `-d`: 백그라운드 실행

### 도입부

- **첫 문장에 해결책(또는 문제)을 즉시 던진다.** 배경·동기 서술을 생략한다.
  - "`NODE_TLS_REJECT_UNAUTHORIZED` 환경변수를 `0`으로 설정해서 인증서 검증을 비활성화 할 수 있다." → 곧바로 코드블록
- 때로는 산문 없이 코드블록으로 바로 시작해도 된다.
- 시그니처 종결은 가능형 **"~할 수 있다"** 다.

### 구성

- `##` 헤딩은 "작업 단계 라벨"로만 쓴다. `###` 이하 깊은 중첩은 쓰지 않는다.
- 코드블록 안에 **파일 경로/설명을 인라인 주석으로 박는다**: `// ~/.claude/settings.json`, `# ~/.ssh/config`.
- 마무리는 긴 결론 문단 없이 **참고 링크 불릿** 또는 **결과 코드블록**으로 자연 종료한다. 굳이 닫을 땐 한 문장으로.

### frontmatter

```mdx
export const metadata = {
  title: "글 제목",
  createdAt: "YYYY-MM-DD",
  tags: ["Next.js"],
};
```

note는 `description`을 두지 않는다.

---

## 4. 일관성을 위해 통일할 것 (시기별로 흔들렸던 항목)

기존 글에서 시기마다 달랐던 부분이다. 앞으로는 아래로 통일한다.

- **마무리 헤딩은 `## 마무리` 하나로 통일한다.** 그동안 `정리`/`마무리`/`마치며`/`후기`/`결론`이 섞였다. 핵심 재요약이든 소감·교훈이든 헤딩 명칭은 `## 마무리`로 쓴다. (note는 마무리 헤딩 없이 종료)
- **완곡 표현 "~것 같다"를 남용하지 않는다.** 초기 글에서 과도하게 잦았다. 본인이 검증한 사실은 단정형으로 쓰고, "~것 같다"는 정말 주관적 소감일 때만 쓴다.
- **코드블록 언어 태그를 실제 언어로 정확히 단다.** 과거 `jsx`/`tsx`/`typescript`가 혼용됐다 — 파일이 TS면 `typescript`/`tsx`로.
- **`#`(h1)을 본문 헤딩으로 쓰지 않는다.** 일부 초기 글에서 마무리를 `#`로 단 예외가 있었다.
- **이미지 alt를 파일명 그대로 두지 않는다.** 항상 설명형 캡션으로.

---

## 5. 이미지

- 이미지는 글과 같은 폴더(`src/contents/{type}/{slug}/`)에 두고, 본문에서 `![설명형 캡션](파일명.png)` 상대경로로 참조한다. (alt 캡션은 2번 article 구성 규칙을 따른다.)
- **PNG 또는 GIF만 사용한다.** 빌드 시 `scripts/copy-images.mjs`가 PNG를 WebP로 변환하고 GIF는 그대로 복사하며, `next.config.mjs`의 remark 플러그인이 `![](foo.png)`를 `/images/content/{type}/{slug}/foo.webp`로 재작성한다. **jpg·svg·avif 등 다른 포맷은 처리되지 않아 이미지가 깨진다.**
- 외부 이미지 URL은 본문 마크다운 이미지(`![](https://...)`)로 쓰지 않는다. 경로가 `/images/content/...`로 재작성되어 깨진다.
