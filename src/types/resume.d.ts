export type ResumeContentType = "career" | "project";

export type ResumeContentMetadata = {
  title: string;
  title: string;
  description?: string;
  position?: string;
  team: string;
  startDate: string;
  endDate?: string;
};
